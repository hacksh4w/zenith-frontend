import React from "react";
import { ContainerStyles } from "../../palette";

const Landing = () => {
  return (
    <ContainerStyles
      sx={{
        minWidth: "100vw",
        minHeight: "100vh",
      }}
    >
      Landing
    </ContainerStyles>
  );
};

export default Landing;
